<UserControl x:Class="PboExplorer.Modules.Startup.Views.ApplicationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PboExplorer.Modules.Startup.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <CheckBox Grid.Row="0" IsChecked="{Binding ConfirmExit}">Confirm application exit</CheckBox>
            <CheckBox Grid.Row="1" IsChecked="{Binding OpenFullscreen}">Open application maximized</CheckBox>
        </Grid>
    </Grid>
</UserControl>
